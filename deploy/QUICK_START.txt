==========================================
QUICK START GUIDE - RINGKASAN LANGKAH
==========================================

📋 CHECKLIST LANGKAH DEPLOY:

□ 1. UPLOAD FILE VIA FILEZILLA
   → Upload folder deploy/ ke /public_html/monitoring/
   → Termasuk: server/, public/, package.json, env_file.txt
   → Rename env_file.txt menjadi .env (di File Manager cPanel)

□ 2. CREATE NODE.JS APP DI CPANEL
   → Buka Node.js Selector
   → Klik "CREATE APPLICATION"
   → Isi form:
     * Node.js version: Pilih terbaru (18.x atau 20.x)
     * Application mode: Production ✅
     * Application root: /public_html/monitoring
     * Application URL: monitoring.solusicodekata.com
     * Application startup file: server/index.js
   → Klik "CREATE"

□ 3. CATAT PORT
   → Setelah create, catat PORT yang diberikan cPanel
   → Contoh: 5001, 5002, dst

□ 4. UPDATE FILE .env
   → Edit file .env di /public_html/monitoring/
   → Ganti PORT=5000 dengan PORT dari cPanel
   → Save

□ 5. TAMBAHKAN ENVIRONMENT VARIABLES
   → Di halaman Node.js App
   → Scroll ke "Environment variables"
   → Klik "+ ADD VARIABLE"
   → Tambahkan:
     * DB_HOST = localhost
     * DB_USER = solz1468_solkit
     * DB_PASSWORD = DemiAllah@1
     * DB_NAME = solz1468_monitoring
     * NODE_ENV = production
     * PORT = [PORT_DARI_CPANEL]

□ 6. INSTALL DEPENDENCIES
   → Klik "NPM Install" di Node.js App
   → Tunggu sampai selesai

□ 7. RESTART APLIKASI
   → Klik "Restart App" di Node.js App
   → Tunggu status menjadi "Running"

□ 8. TEST
   → Buka: https://monitoring.solusicodekata.com
   → Test semua fitur aplikasi

==========================================
FILE PENTING:
==========================================

📄 UPLOAD_INSTRUKSI.txt → Cara upload via FileZilla
📄 SETUP_CPANEL_LANGKAH.txt → Detail form Create Application
📄 AFTER_CREATE_APP.txt → Langkah setelah create app ⭐
📄 UPLOAD_ENV.txt → Cara upload file .env

==========================================

