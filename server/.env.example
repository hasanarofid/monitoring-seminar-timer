# Konfigurasi Database MySQL
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=monitoring_seminar

# Port untuk server backend
PORT=5000

# Konfigurasi tambahan (opsional)
# NODE_ENV=development

# Catatan:
# 1. Copy file ini ke .env dengan perintah: cp .env.example .env
# 2. Edit file .env dan sesuaikan konfigurasi database Anda
# 3. Pastikan database MySQL sudah dibuat (gunakan database.sql)
# 4. Jangan commit file .env ke git (sudah ada di .gitignore)
