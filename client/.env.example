# URL untuk API Backend (dengan /api di akhir)
REACT_APP_API_URL=http://localhost:5000/api

# Catatan:
# 1. URL harus berakhir dengan /api karena axios baseURL akan digabung dengan endpoint
# 2. Socket.IO akan otomatis menghapus /api dari URL ini
# 3. Copy file ini ke .env: cp .env.example .env
# 4. Restart development server setelah mengubah .env
